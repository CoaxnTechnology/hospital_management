# =====================================================================
# Echo App — Environment Configuration
# =====================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control.
# =====================================================================

# -- Domain & Network --
SUBDOMAIN=echo.yourdomain.com
VPS_IP=0.0.0.0

# -- Django --
SECRET_KEY=change-me-generate-with-python
DEBUG=False

# -- Database --
DB_NAME=echoapp
DB_USER=django
DB_PASS=Owbq4rT4_RqC
DB_HOST=127.0.0.1
DB_PORT=5432

# -- Redis --
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# -- Memcached --
MEMCACHED_HOST=127.0.0.1
MEMCACHED_PORT=11211

# -- SSL / Certbot --
CERTBOT_EMAIL=admin@yourdomain.com

# -- Admin User (optional) --
ADMIN_USER=admin
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASS=secure_password_here

# -- DICOM Ports --
DICOM_WORKLIST_PORT=11112
DICOM_STORE_PORT=11113
